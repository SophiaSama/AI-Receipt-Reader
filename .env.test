# Frontend env for E2E ("test" mode). Loaded by `npm run dev:e2e` (vite --mode test).
# Points the app at the LOCAL disposable Supabase stack (Option A) so seeded
# users in supabase/seed.sql can log in. These are the standard local dev keys
# (fixed demo JWT secret) and only reach http://127.0.0.1:54321 — safe to commit.
# Your normal `npm run dev` keeps using the cloud values from .env / .env.local.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
