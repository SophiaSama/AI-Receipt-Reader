name: Integration Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 22.x]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies (includes backend build via pretest)
      run: npm install
      # npm install runs lifecycle scripts including pretest
      # pretest automatically builds backend before tests

    - name: Run tests
      run: npm test -- --run
      # pretest hook already built the backend
      env:
        USE_LOCAL_STORAGE: true
        NODE_ENV: test
        MISTRAL_API_KEY: test-key

    - name: Upload coverage reports
      uses: codecov/codecov-action@v4
      if: matrix.node-version == '20.x'
      with:
        files: ./coverage/coverage-final.json
        flags: integration-tests
      continue-on-error: true

  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: 'npm'

    - name: Install dependencies
      run: npm install
      # npm install runs lifecycle scripts

    - name: Check TypeScript
      run: npx tsc --noEmit

    - name: Check backend TypeScript
      run: |
        cd backend
        npm install
        npx tsc --noEmit
